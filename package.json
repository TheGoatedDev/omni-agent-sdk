{
	"name": "omni-agent-sdk",
	"private": true,
	"license": "MIT",
	"packageManager": "pnpm@9.15.0",
	"repository": "https://github.com/thegoateddev/omni-agent-sdk",
	"scripts": {
		"build": "turbo run build",
		"check": "turbo run check",
		"lint": "biome check .",
		"format": "biome format --write .",
		"test": "turbo run test",
		"changeset": "changeset",
		"release": "changeset publish",
		"ci:version": "changeset version && pnpm install --lockfile-only",
		"ci:publish": "changeset publish"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@changesets/cli": "^2.27.0",
		"turbo": "^2.3.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
